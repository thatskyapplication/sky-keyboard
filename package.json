{
	"private": "true",
	"type": "module",
	"scripts": {
		"build": "tsup",
		"test": "tsc --noEmit",
		"format": "biome check --write",
		"lint": "biome check"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.0",
		"tsup": "^8.5.0",
		"typescript": "^6.0.3"
	},
	"devEngines": {
		"runtime": {
			"name": "node",
			"version": ">=26",
			"onFail": "error"
		},
		"packageManager": {
			"name": "pnpm",
			"version": "11.10.0",
			"onFail": "error"
		}
	}
}
